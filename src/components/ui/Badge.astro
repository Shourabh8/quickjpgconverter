---
import { cn } from "@lib/utils";

interface Props {
  variant?: "brand" | "success";
  class?: string;
}

const { variant = "brand", class: className } = Astro.props;
---

<span class:list={[variant === "brand" ? "badge-brand" : "badge-success", className]}>
  <slot />
</span>
